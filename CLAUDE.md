# CLAUDE.md — gis-md 项目规则

## 北极星文档:开工前读,收官后更新

`docs/northstar/<模块>.md` 回答**「这个模块做到什么程度算好、现在到哪了」**。
三类文档分工不同,别混:

| 文档 | 回答 | 性质 |
|---|---|---|
| `AI_INDEX.md` | 代码在哪(结构/行号) | 有 ctest 守卫,行号漂移会红 |
| `docs/issues/*` | 当时怎么修的 | 写完即冻结的历史档案 |
| **`docs/northstar/*`** | **做到什么程度算好** | **活文档,随专项收官更新** |

现有:
- `vector.md`(矢量:底图三分工 / 符号标注 / 性能账 / 已判死清单)
- `terrain.md`(地形:几何质感 / 加载期 / 源覆盖兜底 / 跨平台 / 性能债)
- `lighting.md`(光照/颜色:天空 / 大气雾 / 日落着色 / HDR 线性管线 / 技术债)
- `camera.md`(相机/手势:pin 保锚 / 轴隔离 / 惯性 / 碰撞约束 / tilt 灵敏度 / 真机闭环基建)
- `imagery.md`(影像:缺失退化祖先兜底 / 缓存网络 / GCJ 配准 / 合规 / 性能债)

**编号命名空间**:`vector.md` 用无前缀 `V*`/`P*`(历史既有,不动);
`terrain.md` 用 `T-V*`/`T-P*`;`lighting.md` 用 `L-V*`/`L-P*`;`camera.md` 用 `C-V*`/`C-P*`;
新建模块文档照此加前缀(imagery → `I-`)。
理由:无前缀会让「V3」跨模块歧义。

### 必须读的时机

动手前,若改动涉及某模块的**用户可见行为**(渲染观感、交互手感、性能特征),
先读该模块北极星文档,并**在动手前说明「本次动 V几」**——让用户能在错误前提上
立刻打断。纯内部重构、构建脚本、测试工具不需要。

模块尚无北极星文档、而本次工作产生了可判定的体验要求 → **新建一份**(照 `vector.md` 形态)。

### 必须更新的时机

- **专项收官** → 改判据状态(✅ 达成 / ⚠️ 有缺口 / ❌ 未做 / 🔒 待用户拍板),
  附证据(commit / 真机数据 / 截图)。不许凭印象改。
- **新增开销** → 同一次提交更新「代价」列;测不了就进性能债表(带 `P` 编号)。
  **不许填"应该很小"**——空头承诺会沉淀成债。
- **用户提出新体验要求** → **立刻加一条判据**(哪怕状态是 ❌)。
  没写下来的要求会在下一次会话丢失,这是这套文档存在的首要理由。
- **方案被否决** → 记进「已判死 / 勿再提」节,附死因。防止日后重复提议。

### 引用协议

`V`(判据)/ `P`(性能债)编号是**跨会话稳定锚点**。
用户说「V5 不满意」即指该条,不必重新描述观感;我提议改动时同样用编号指代。
**编号只增不改**——改编号会让历史对话失效。

### 判定权归属

- 【机制】类判据:我自证(命令输出 / 计数 / 测试红绿 / 帧时),不占用用户。
- 【观感】类判据:**像素判断归用户**。我只负责钉死复现场景 + 给截图,
  不替用户判定"好看"。

## 帧收敛申报纪律(按需渲染契约,2026-08-18)

引擎是**按需渲染**(帧门控,idle 即停帧)。任何"依赖帧循环推进的异步收敛"
——每帧消化的队列、逐帧推进的动画/fade、按帧节流的重算——**必须向帧门控
申报"我还在收敛"**,否则停帧饿死,症状是"某东西不出现/停在半程"且零报错。
V27 家族一天连出五洞(placement 供给/fade/字形烘焙预算/贴地重钳/换代竞态),
全是这同一结构病。

新增此类环节时三选一:
1. `WorkLedger` 票(`Kind::Pumped`,照 `TerrainPageStore::syncWorkTicket`
   reconcile 模式);
2. 并入既有收敛谓词(如 `FeatureRenderLayer::hasPendingLabelWork`),
   谓词必须是**状态查询**(免疫末帧竞态)且**有终止态**(不许恒真白烧);
3. 宿主侧 `requestRender`(仅限引擎看不见的 app-owned 资源,mvtPending 先例)。

配套禁令与兜底:
- **禁止帧数节流**(`frameId >= last + N`):按需渲染下帧是稀缺资源,帧数
  不再是时间的代理,改 wall-clock 秒。
- **兜底网 = ShadowVerify**(demo 已默认开):idle 判定后画面仍在变即报
  Error——漏申报的构造性捕网。真机验收流程看它无 Error 行;
  它报警时先怀疑"谁忘了申报",再怀疑画面本身。
