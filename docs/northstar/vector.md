# 矢量模块北极星 — 产品体验判据

**这份文档回答「做到什么程度算好、现在到哪了」。**
不回答「代码在哪」(那是 `AI_INDEX.md`),也不回答「当时怎么修的」(那是 `docs/issues/*`)。
`docs/issues/` 是写完即冻结的历史档案;**本文是活的**,随每次专项收官更新。

## 怎么用(这是本文的主要价值)

- **判据编号(V1…)是稳定引用锚点。** 你可以直接说「V3 我不满意」「V9 提前做」,
  不必重新描述一遍观感;我开工前会引用「本次动 V3/V4」,收官后改状态并附证据。
- **状态**:✅ 达成(有证据) ⚠️ 部分达成/有已知缺口 ❌ 未做 🔒 待你拍板
- **类型决定谁说了算**:
  - 【机制】我能自证 —— 命令输出、计数、测试红绿、帧时。我负责给数,不需要你看。
  - 【观感】像素判断**归你** —— 我只负责钉死复现场景 + 给截图,不替你判定"好看"。
- **「代价」列是硬要求**:每条体验都要写清花了多少(GPU/CPU/内存),因为你认可的
  准则是「体验达标**且最便宜**」——只写达没达成,就没法判断值不值、还有没有余量。
  **没量化过就写「未量化」,不许填"应该很小"**;这类空头承诺会沉淀成 C.5 的债。
- **性能债编号(P1…)** 与判据编号同为引用锚点,见 C.5。
- **改状态必须附证据**(commit / 真机数据 / 截图),不能凭印象改。
- **`推断` 标记**:凡带此标记的判据是我从现状与对照系推出的,**不是你说过的**,
  优先请你校对——校错了比没写更糟。

---

## 北极星一句话

> 在 **45° 斜视 + 地平线在屏**的 3D 地形上,线的观感**超过 maplibre 的任何一种模式**:
> 拿到平面模式的线宽恒定与锐度,同时不付平面模式"没有 3D"的代价,
> 也不落进地形 RTT 模式"两次重烘之间线宽在漂"的病。

**这不是口号,是机制差异,已在 `.ref` 源码核实**(见下方对照表)。
对照系为什么是 maplibre 而不是 Google Earth:GE 的矢量是预烘影像,
我们要的是**运行期可换样式/可编辑**的矢量,GE 那条路不可比。

### 水位线对照(三方)

| 维度 | maplibre 平面模式 | maplibre 地形 RTT | **本引擎** |
|---|---|---|---|
| 线宽随相机连续恒定 | ✅ 屏幕空间逐帧画 | ❌ **两次重烘之间是位图拉伸** | ✅ **FS 内逐像素解析解算** |
| 重烘触发 | 不需要 | 源瓦集合/数据版本变才重烘,**指纹不含 zoom** | **不需要**(z 封顶后零重烘) |
| 3D 地形贴合 | ❌ 没有地形 | ✅ | ✅ |
| 任意俯仰角 / 地平线在屏 | — | 受限(病被放大故回避) | ✅ 45° 斜视为默认姿态 |
| 放大时锐度 | ✅ 矢量重画 | ❌ 纹理放大糊 | ✅ SDF 解析放大(**V5 有余债**) |
| GPU 成本 | 独立 draw | 独立 RTT pass + FBO | **~0(寄生地形 FS)** |

> **关键出处**:`.ref/maplibre-gl-js/src/webgl/render_to_texture.ts:114-127` ——
> RTT 失效指纹 = `sorted_tile_keys#revision`,**只含源瓦集合与数据版本,不含 zoom
> 或任何相机参数**。故 z14.0 连续推到 z14.9 的全过程 RTT 不重烘,线宽按纹理比例
> 放大 = 屏幕像素不恒定。这是它的结构性病,不是参数没调好。

### 「超越」的兑现状态(2026-08-14 晚,D2 落地后)

| 已兑现(机制自证) | 未达标 |
|---|---|
| 线宽像素恒定(V3,**全画面**,兜底已删无处可躲)、零重烘(V14)、~0 GPU(V2)、3D 任意俯仰角、深放大连续锐利 | 观感项待拍板(V4 ramp 标定 / V5 像素判定);V9 dash / V20 Metal 未做 |

> 历史教训留档:上上版曾在兜底接管 92% 画面时写"✅ 已兑现"(E3 案)。
> D2 的修法不是"验证了兜底没生效",而是**删掉兜底**——不存在的路径不需要验证。

---

## A. 底图三分工(面 / 线 / 点)

架构准则(你认可过):**矢量=数据模型,渲染表示是引擎内部自由;表示随负载,
判据=体验达标且最便宜**。

| # | 判据 | 类型 | 状态 | 代价(实测) | 证据 / 差距 |
|---|---|---|---|---|---|
| **V1** | 面(水/landuse)贴地无浮、与影像同轨合成 | 机制 | ✅ | **GPU ~0**(并进影像合成轨,无独立 pass);CPU compose ~17ms/帧 @churn,**在 worker 不占渲染线程** | 刀1 drape 进页存储,面 ~75ms GPU 归零(`4bc3e815a`) |
| **V2** | 线(路网)贴地、掠视无块状、交叉口无断裂 | 观感 | ✅ | **GPU ~0**(寄生地形 FS,terrain pass 3.3ms = 纯影像基线);烘焙**亚毫秒/页**(CPU worker scatter) | 刀2 SDF 场 + 地形 FS 解算(`9d798ecb6`);stencil 链退役 |
| **V3** | **线宽真屏幕像素恒定**:近景/远景/祖先页兜底/页界跳档,同级路一样粗 | 机制 | ✅ | 场显存 16MB 封顶(64 层 RGBA8);FS ~1.6 fetch(哨兵早退)+ 胶囊解算 | **D2 重修达成(2026-08-14 晚)**:两个纹素下限**已删除**——不存在兜底可躲(E3 元规则的结构性满足),ramp 解算是唯一路径;host 解算器单测锁精确重建(±0.08 texel),真机三距离段+深放大截图验收 |
| **V4** | **线宽分级连续**:干道粗、支路细,随相机连续变化无台阶 | 观感 | 🔒 | 同 V3(同一次解算) | **D2 后 ramp 成为唯一宽度权威**(下限退役);机制生效已自证,**停点/端点观感待你拍板**——注意:你此前所有宽度观感判断都是在"下限接管"状态下做的,需重新校准 |
| **V5** | 超封顶近景线仍锐利 | 观感 | ✅ | **负成本**:AA 恒 0.5px,无放大羽化 | **用户拍板通过(2026-08-14 晚)**:D2 深放大(250m)截图「锐度可以了」;E3 案三条判据至此全部收口 |
| **V6** | 建筑:近=3D 挤出 / 中=drape 块 / 远=剔除 | 观感 | ⚠️ | 现状含在 V1 的 ~0 内;3D 挤出成本**未评估** | 现只有 drape 灰块;**3D 挤出未做**(定案为未来内容,非贴地负载) |
| **V7** | 点符号(POI)分类图形/颜色,贴地锚定正确 | 观感 | ✅ | billboard 几何链;rank 截断 **128 符号/瓦** 兜住密度 | 符号五刀 刀A+刀E(`f60fec313`) |
| **V8** | 文字标注:CJK/中英混排、避让不重叠、跨瓦不跳 | 观感 | ✅ | build p50 **2.63→1.51ms**;placement **300ms 节流**、单次全量 <1ms(千级候选) | 刀B(GlyphAtlas 2048²/32px)+刀C(crossTileID)+刀D(节流+渐变) |
| **V9** | 虚线 / dash 样式 | 机制 | ❌ | 预计 ~0(FS 内沿线参数化) | 首版单色实线;场编码支持,FS 未做 |
| **V10** | 与影像**精确对齐**(GCJ):任意 zoom / 瓦界 / 祖先页不错位 | 机制 | ✅ | CPU 逐顶点变换(烘焙期,已含在亚毫秒/页内) | 逐顶点 GCJ(`dbeb08f58`)+瓦界四角仿射(`80892aca1`,29.6m 错缝根修) |

## B. 稳定性(运动态才暴露的那一类)

> 教训已写进代码注释:**「静态截图不闪」≠「运动态不闪」**。本组判据一律
> 要求动相机 + 掠视大 cell 验证,静止截图不作数。

| # | 判据 | 类型 | 状态 | 代价(实测) | 证据 / 差距 |
|---|---|---|---|---|---|
| **V11** | 运动/churn 期路网不闪 | 观感 | ✅ | 零(修的是漏拷,不是加东西) | 三轮根修,真根因=合批漏拷场 uniform(`9043e20fe`);契约守卫已锁 |
| **V12** | 拉远/换代时**线与点/面**均不整片消失 | 机制 | ✅ | 线:锚页预暖(不变);点/面:R* 选择层记账,~0 | 线=锚页预暖(`04f6fca54`);点/面=R* 后代回退+存货保活,host 测试 `ZoomOutFallsBackToLoadedDescendants`/`StandinTilesAreRetainedUnderBudgetPressure`(2026-08-15) |
| **V13** | 符号/标注换代无闪 | 观感 | ⚠️ | 渐变逐帧 advanceFades,~0;opacity 回写见 P8 | **硬闪已根修**(`3f86f0234`):换桶后新桶标签顶点 opacity=0 而回写被 300ms 节流吞掉 → 最长黑 300ms。修=bake 时直接烘入 placement 当前 opacity。**根因口径**:节流该管碰撞重算,不该管 opacity 回写(maplibre 同款解耦,见 B.4)。观感待复看 |
| **V25** | **标注离场淡出**:带符号的瓦离开视口时标签淡出而非硬消失 | 观感 | ❌ | 未做。预计 = 瓦多驻留一个 fadeDuration(~300ms)的内存 | maplibre 有:`tile_manager.ts:588-598` 对 `hasSymbolBuckets` 的瓦 `setSymbolHoldDuration` 持有到淡出结束才删。我们 `MvtVectorSource` 的 `toDrop`(整片离场)立即 drop;R* 原子换手只覆盖"有替换者"那一路。**当前视野内手势不触发**,故非 V24 主因 |
| **V14** | 拉近时场不重烘(overzoom 解耦) | 机制 | ✅ | **省**:场上传降到影像的 **~7%**(20+9 vs 208+167 项);显存 32→10MB | 步3(`0fb90a919`) |
| **V24** | **连续缩放期点/线不闪**,且不伴随无谓重建 | 观感+机制 | 🔒 | R* 选择层 ~微秒级/帧;过渡帧后代拼贴 draw ≤64×/理想瓦(封顶 3 级,收敛归零) | 机制半已自证:R* 根修(B.3),host 24 条测试锁不空窗/不重影/存货保活;**观感半待用户**:真机 release 已装,等像素判定(设备锁屏未能代驱)。历史:发现时 `adb reverse` 空(环境假象嫌疑),见 B.1 |

### B.1 V24 排查记录(2026-08-15,未结案)

**⚠️ 先记环境坑**:发现时 `adb reverse tcp:8092` **已静默失效**(USB 重连的老坑,
记忆里记过一次「矢量全灭假象」)。此状态下缩放拉新瓦全部失败 =
点线成片出现又消失,**与"闪"的表象完全一致**。这是第一嫌疑,已恢复待复看。

**已排除(修好环境后,debug 包,重庆默认位姿,连续放大 12 次 ≈2.87 级):**

| 嫌疑 | 实测 | 判定 |
|---|---|---|
| 网络/请求积压 | MVT 仅 4 请求、**全 200**、排队 p50=0ms | **无辜** |
| 场重烘(V14 回归) | `fields=0` 整段缩放零场上传 | **无辜**,V14 保持 |
| overzoom 越界请求 | 请求 z 最高 14,无 z15+ | **无辜**,封顶正确 |
| 静止期浪费 | 静止 8s:MVT 请求 0、无 compose | **无辜**,脏驱动有效 |

**尚未判定**:`tick60 items=38`(60 帧内 38 次页合成)。**现有计数器不区分
"新页首次合成"与"同一页重复合成"**——后者才是浪费,正是 P1(compose 源集合
指纹缺失)记的债。要判定必须先加"重合成"计数器。

**一条方向性线索**:点符号**不走 pageStore compose**(billboard 直接画),
所以 items 再高也解释不了"点闪"。点闪的机制嫌疑在符号链的 crossTile 匹配 /
fade 时序(即 V13 那条已知缺口:像素级录屏验收从未做)。
线与点同时闪 → 共同上游嫌疑 = 瓦片 LOD 换代本身。

### B.2 换代代码排查(2026-08-15):线与点走的是两条路,只有一条有保护

查 `VectorTileTree` / `MvtVectorSource` / `RoadFieldSource` 后的**结构性发现**:

| | 线(路网) | 点 / 标注(+ drape 面) |
|---|---|---|
| 取瓦 | `RoadFieldSource` **直接问 `MvtTileFetchCache`** | `MvtVectorSource` → **`VectorTileTree`** |
| 表示 | pageStore 场页(z 封顶) | 逐瓦网格 + 符号实例 |
| 换代保护 | 两级锚页预暖、z 封顶零重烘 | **无迟滞、无后代回退、无保活** |

> **`VectorTileTree` 不参与线的渲染。** 这条以前没写清,导致 V12(拉远/换代时线
> 不整片消失)拿场页锚页预暖当证据标 ✅ —— 对线成立,但**同一句话对点从不成立**,
> 而点走的是完全没保护的另一条路。这是 E3「兜底路径冒充主路径」的同型问题:
> **一层的修复被记在了一条跨层判据的账上**。

**四个缺陷,三个已由 host 测试钉死**(`tests/unit/data/test_vector_tile_tree.cpp`,
测试**钉的是当前缺陷行为**,修好后会红 —— 那时改判据,别改测试迁就实现):

| # | 缺陷 | 位置 | 证据 |
|---|---|---|---|
| **①** | **zoom 选择无迟滞**:`floor(log2(4e7/h))` 硬切,视高抖 ±0.5% 就翻档 | `VectorTileTree.cpp:31-37` | `ZoomHasNoHysteresisAtBoundary`:20 次微抖 → **20 次全翻档**,零死区 |
| **②** | **只有祖先回退,没有后代回退**:拉远时粗瓦未加载 → 渲染集**空**,哪怕细瓦还在缓存、几何完全够覆盖 | `.cpp:94-104`(只 `parent()` 向上走) | `ZoomOutHasNoDescendantFallback`:z14 喂满后拉到 z11,`renderTiles` **空** |
| **③** | **出渲染集一帧即成淘汰候选** | `.cpp:204`(`lastUsedFrame < frame_`) | `TilesLeavingRenderSetAreImmediatelyEvictable`:超预算后原渲染瓦被淘汰 → 抖回来要重拉 |
| **④** | **drop 无限速 / commit 限速 4**:出集瓦**一帧全掉**,新瓦每帧最多 `maxTileCommitsPerUpdate=4` 个进 | `MvtVectorSource.cpp:148-176` | 未单测。视口上限 `maxTilesPerView=64` → 整片换代最坏 **16 帧 ≈267ms** 才补齐 |

**①+④ 是"闪"的主机制**:边界抖动触发整集换代,而换代天生不对称(旧的瞬间没、
新的慢慢来)。**②+③ 是"浪费"那一半**:抖回来时数据已被丢弃,必须重镶/重拉 ——
正好对上你说的「闪 = 可能在浪费」。

### B.3 R* 落地(2026-08-15,同日根修)

**方案 R\* = 置换式细化 + 全有全无回退 + 置换单元原子换手**,一次吃掉缺陷
②③④⑤(①迟滞降级为"省重算",见下)。语义对拍本仓库地形 Tileset 的
replacement refinement 与 maplibre `_updateRetainedTiles`,但比 maplibre 多
"无重叠"保证 —— 世界空间渲染无逐瓦 stencil,祖先与子瓦同框即要素重影,
maplibre 靠裁剪掩、我们靠**选择层不产生重叠**。

| 层 | 改动 | 消掉 |
|---|---|---|
| `VectorTileTree::update` | 重写为递归置换细化:quad 在视口内凑不齐就画自己(已加载才行),凑得齐才画子树;renderTiles 恒为**精确覆盖**;沿途存货全 touch 保活;理想层缺瓦照常请求(收敛义务) | ②(后代回退,跳档拉远细瓦顶住)③(存货保活)⑤(无重叠) |
| `MvtVectorSource::update` | 出集分"离场瓦"(立即 drop)与"占位者"(等替换内容全就绪,同帧 commit+drop 原子换手);单元不可拆,每帧至少放行一个,宁超预算不劈两帧 | ④(空窗)+ 换手期重影 |

**证据**:host 测试 24 条全绿(树 15 + 源 9),含
`RenderTilesAreExactCoverDuringTransition`(三个部分到达过渡态逐一验
无重叠+无空洞)、`LodSwapIsAtomicNoHoleNoOverlap`(粗→细→粗全程逐 update
验不空窗不重影,**守卫已故意破坏验响**:恢复旧 drop 行为立刻红"空窗");
全量 ctest 187/187。

**遗留(记账不假装)**:
- **① zoom 无迟滞仍在**(`ZoomHasNoHysteresisAtBoundary` 保留钉现状)。
  R* 后翻档不再产生空窗(存货直接顶),迟滞从"必需"降为"省重算";
  真机若见边界高度反复换代耗电/耗 CPU 再做,几行的事。
- **换手瞬间的内容跳变**(粗细两代符号密度/rank 截断不同)是 LOD 数据固有,
  不是时序缺陷;fade 掩饰属观感策略,待用户看过 R* 后的真机再定。
- **U1(无名符号跨代身份)判不需要**:图标顶点 9 float 无 opacity 分量,
  不参与 fade/placement,其闪即桶空窗,R* 已根除;同一 POI 两代锚点一致,
  换手帧像素连续。有名标签本就有 `crossTileIdFor`。**勿再提**"给图标加
  跨代身份"——没有可服务的机制。
- 过渡帧后代拼贴 draw 数最坏 4^3=64 倍/理想瓦(选择层 ~5k 哈希,微秒级;
  draw 压力靠 `maxDescendantStandinLevels=3` 封顶),收敛即归零。

**测量作废记录**:首轮用 `KEYCODE_6`(6km 掠视位姿)复现,请求落在 y=6728-6729,
而数据覆盖 y=6770-6790 —— **位姿在重庆以北约 40 个瓦,整片没数据**,该轮数据全废。
复现缩放必须用启动默认位姿。

### B.5 R* 全有全无回退**已撤销**(2026-08-15,用户报"点全部消失")

**这是我当天引入又当天撤回的回归,死因记全,别再翻回去。**

**症状**:当前视野内点(POI)几乎全没。**证据链**:真机 `TileSymbol commit`
里 z8/z10/z11 混现,而 z8(203,105)正是 z11(1628,846)的祖先 ——
一张 z8 瓦只有 42 个 POI,它覆盖的 4 张 z11 瓦合计 312 个。

**机制**:全有全无回退是**整支回滚** —— quad 里缺一块,已加载的兄弟全部
作废退到祖先;而**中间层从不被请求**(只请求理想层),回滚遂一路级联到
某个碰巧还在缓存里的很粗祖先。一块 z11 缺失 → 整棵 z8 子树(64 张 z11 的
内容)降级成一张 z8。

**判断错在哪(可复用的教训)**:我把"无重叠"当成硬要求,而它的严重性
**取决于这棵树喂什么内容**。查 `GLESView.cpp` 才确认:
**`includeLayers={"poi"}` —— 本树只喂 POI 符号**(面走 drape 页存储、
线走 SDF 场,都不经这里)。对符号,重叠 = 同一个 POI 画两遍(标签还有
crossTileID + 碰撞去重兜着),是轻伪影;我却为消它付了内容量掉一个数量级
的代价。**定"硬要求"之前先查这条路径实际承载什么** —— 缺陷⑤ 的严重性
是从多边形/线的直觉外推的,而这棵树上根本没有多边形和线。

**保留 / 撤销**:

| R* 组成 | 处置 | 理由 |
|---|---|---|
| 后代回退(缺陷②) | **保留** | 拉远跳档时细瓦整块顶住;仍要求完整覆盖(半个 quad 会在理想瓦内留硬边界,比重叠难看) |
| 存货保活 touch(缺陷③) | **保留** | 纯赚,无副作用 |
| 置换单元原子换手(缺陷④,`MvtVectorSource`) | **保留** | 消空窗,与选择层正交 |
| **全有全无 / 精确覆盖(缺陷⑤)** | **撤销** | 见上。重叠回到"可忍受"清单 |

**守卫**:`IncompleteQuadKeepsLoadedSiblings`(缺一块时已加载兄弟必须全部
上屏)、`RenderTilesCoverWithoutHolesAndKeepLoadedFine`(无空洞 + 不丢细瓦)。
真机复验:换代只出理想层 z14(8 瓦 125 符号),不再掉到 z8/z10。

### B.4 maplibre 标注为何不闪(2026-08-15,`.ref` 源码核实)

问的是"它凭什么不闪",答案是**四道防线,而不是一个技巧**。逐条对照:

| # | maplibre 机制 | 出处 | 我们(修前 → 修后) |
|---|---|---|---|
| **1** | **opacity 真源不在桶顶点**,在 `Placement.opacities`(按 `crossTileID` 键的 map);桶顶点只是 GPU 镜像 | `placement.ts:185,963-972` | 修前**真源就是顶点**(新桶=0 即真的黑);修后仍在顶点,但 bake 时从 placement 拉取 → **等价** |
| **2** | **新桶到达当帧立即重写 opacity**:`if (placementCommitted \|\| symbolBucketsChanged)` —— `symbolBucketsChanged` 就是"桶变了"这一路,**不等节流** | `style.ts:1897` | 修前**只在节流窗或有 fade 推进时回写** → 换桶后最长黑 300ms = 你看到的硬闪;修后 bake 时直接烘入 → **等价** |
| **3** | **`justReloaded` → `skipFade`**:重载桶上没有历史 id 的符号直接满亮度,不重新淡入 | `placement.ts:819,33` | 我们无此语义:新 id 仍从 0 淡入。**判不跟进**——crossTileID 匹配上的已继承,没匹配上的本就是新符号,淡入是正确观感 |
| **4** | **带符号的瓦离场不立即删**,`setSymbolHoldDuration(fadeDuration)` 持有到淡出结束才移除 | `tile_manager.ts:588-598` | **仍缺**:`toDrop` 里整片离场的瓦立即 `drop` → 标签硬消失无淡出。R* 的原子换手只覆盖"有替换者"那一路。记为 V25 |

**最关键的一条是 #2,而它暴露了一个口径错误**:maplibre 的 300ms(`fadeDuration`,
`map.ts:511`)节流的是 **`stillRecent` → 碰撞重算**(`placement.ts:1268`),
**不是 opacity 回写**。我们把两件事绑在了同一个闸上 —— 碰撞结果在亚秒尺度稳定
所以节流合理,但 opacity 回写是"让 GPU 追上已知状态",没有任何理由节流。
本次修解开了这个耦合。

> 附带一条口径细节(值得学):`stillRecent` 的节流时长带 zoom 修正 ——
> **缩放进行中用满时长(少跑,避免 jank),停下后才缩短(多跑,快速收敛)**。
> 我们是恒定 300ms。真机若见"停手后标注重排慢半拍"再考虑,现无证据。

**性能债(顺带量出来)**:maplibre 的 opacity 是**独立 1 字节/顶点的 buffer**
(`symbol_bucket.ts:174,210-213`,注释明写"performance hack"用 uint32 写 uint8 读);
我们改一次 opacity 要**重传整条 8 float/顶点的标签顶点流(32B/顶点,32×)**。
未量化,记 P8。

## C. 性能北极星(预算 / 实测 / 债)

> **准则**:优化只走「消除浪费」,**不走降低可见细节**——限帧、降 LOD、缩短可见
> 距离都不算优化。矢量三分工的整个架构就是这条准则的产物:面/线的 GPU 成本不是
> 被"调低"的,是被**换了表示**换掉的。

### C.1 帧预算(60fps = 16.7ms) `推断`

| 阶段 | 预算 | 当前实测(2026-08-14 步3 后) | 余量 |
|---|---|---|---|
| FrameLoop total(手势期,无偏采样) | < 16.7ms | **6.9 / 8.1ms**(n=2,见下方采样陷阱) | 大 |
| engine(CPU 侧) | < 6ms | p50 **3.69ms**,max 7.95ms | 有 |
| **慢帧率**(total ≥ 25ms) | < 1% `推断` | **2.1%**(5/241 帧) | **超** → 见 P6 |
| 矢量专属 GPU pass | 个位数 ms | **0.01ms** | 大 |
| terrain pass(含面+线场解算) | 不高于纯影像基线 | **3.62-4.18ms** vs 刀2 记录基线 3.3ms | 同量级 |
| GPU total(手势期) | < 16.7ms | **7.80-9.02ms**(尖峰帧 13.94) | 有 |

> **⚠️ 采样陷阱(踩过一次,写下来防复发)**:`FrameLoop` 日志的打印条件是
> `frameId<=3 || frameId%120==0 || total>=25ms || swap>=8ms` ——
> **它是异常捕获,不是随机采样**。直接对日志行算 p50 会把慢帧当分母,
> 得出"p50=20.6ms 超标"的假结论(真值 6.9-8.1ms)。只有 `%120==0` 那批无偏。

**预算是我推断的,请校对**:我按 60fps 反推,但你的实际目标可能是 30fps 稳定
优先于 60fps 抖动。这条定不下来,后面所有"还有没有余量"的判断都会偏。

### C.2 三条历史曲线(证明架构换表示是有效的,不是调参)

| 指标 | 改造前 | 现在 | 怎么换掉的 |
|---|---|---|---|
| `vec:mvt-basemap` GPU | **74-142ms**(占 94%,发热真凶) | **0.01ms** | 面→drape 合成(-75ms)、线→SDF 场(-12~16ms)、stencil 链整体退役 |
| GPU total | ~148ms | **36-46ms**(动)/ 6-19ms(静) | 同上 |
| FrameLoop 中位 | 148ms | **11-16ms** | 同上 |

### C.3 当前成本结构(钱花在哪)

| 资源 | 当前占用 | 备注 |
|---|---|---|
| 页存储显存 | 512 层 × 256² × 4B = **128MB 上限**,LRU 实驻 125-185 页 | 影像主体 |
| 场平面显存 | **16MB 封顶**(64 层 RGBA8 D2;实驻 ~25 页 6.5MB) | z 封顶后可见场页远少于影像页 |
| 间接纹理 | 影像 4.2MB + 场 4.2MB(64²×4B×256 各一份) | 步3 新增场 indir 一份 |
| 字形图集 | 2048² / 32px ≈ 4096 字形 | 重庆 POI 实测 1036 唯一字符 |
| MVT 瓦缓存 | **48 瓦**(面/线/符号三方共享,A.5 单一化后) | 冷启动重复拉取 34→25,剩余=容量置换 |
| CPU worker | compose ~17ms/帧 @churn;场烘焙亚毫秒/页 | **均不占渲染线程** |

### C.4 性能判据

| # | 判据 | 类型 | 状态 | 代价(实测) | 证据 / 差距 |
|---|---|---|---|---|---|
| **V15** | 矢量开启时 FrameLoop 稳态 < 16.7ms | 机制 | ✅ | — | 无偏采样 6.9-8.1ms(2026-08-14 实测) |
| **V23** | **手势期慢帧率 < 1%** `推断` | 机制 | ⚠️ | — | 实测 2.1%(5/241,max 187ms);根因未定论,见 P6 |
| **V16** | 矢量 GPU pass 个位数 ms | 机制 | ✅ | — | 0.01ms |
| **V17** | 长时间运行不发热(TETHER 场景) | 机制 | ✅ | — | 发热真凶(面 fill 75ms)已根除 |
| **V18** | 显存有界、缓存不无限增长 | 机制 | ⚠️ | — | 页/场/瓦缓存均有界;**字形图集与 crossTile 索引只增不减**,跨城市漫游会涨 |
| **V19** | 静止时不烧电(脏驱动、无帧不出帧) | 机制 | ✅ | — | 停帧 gating;场在途已纳入 WorkLedger(停帧不饿死场上传) |

### C.5 已知性能债(明记,不假装没有)

| # | 债 | 影响 | 现状 |
|---|---|---|---|
| **P1** | compose 源集合指纹缺失 | churn 期可能做无谓重合成 | 既有 TODO,未量化 |
| **P2** | MVT 缓存 48 容量 vs 175 唯一瓦 | 冷启动仍有 25 次重复拉取 | 调容量即可,唯一治理点 |
| **P3** | 字形图集 / crossTile 索引只增 | 跨城市长时间漫游内存涨 | 已埋哨兵,未触发 |
| **P4** | placement 时间片增量未做 | 千级候选 <1ms,万级会尖刺 | 4ms 告警哨兵已埋,触发再做 |
| **P5** | ~~场线宽专项步1-3 的 FS 增量未量化~~ | — | **已结清**(2026-08-14):terrain pass 3.62-4.18ms,与刀2 记录的 3.3ms 基线同量级,**无数量级回归**。严格 A/B(专项前 commit 编包对拍)**未做**,理由:即使真有 0.5ms 差异也只占 16.7ms 预算的 3%,不改变任何架构决策 —— 精度提升不改变行动,不值得再花编译+采样 |
| **P7** | ~~**D2 单纹素单线容量**~~ | — | **已结清(2026-08-15):量化后判定不修**。详见 [C.6](#c6-p7-容量债量化2026-08-15) |
| **P8** | **opacity 回写重传整条标签顶点流**(8 float/顶点 = 32B/顶点),改一个标签的透明度要传整桶 | 未量化。maplibre 用独立 **1 字节/顶点** opacity buffer(32×),见 B.4 | **不修**(2026-08-15):稳态下 `appliedOpacity` 逐 entry 比较免写,只在 fade 推进期(~300ms 窗)真传;要修需拆独立 buffer + shader 加一路属性,动管线契约。真机见传输尖刺再启 |
| **P6** | **手势期慢帧 2.1%**(5/241,max 187ms),时间**不在引擎**(engine 仅 2.0-3.0ms,swap ~1ms) | 掉帧可见,超出 <1% 预算 | **新发现**(为填本表而测出)。嫌疑区间 = `frameStart→engine` 之间的宿主侧代码,主要嫌疑 `gMvtSource->update()`(符号链视口树更新跑在渲染线程)。**无分段计时,未定论**;归属=demo 宿主层,非引擎矢量链 |

### C.6 P7 容量债量化(2026-08-15)

尺子:`scaffold/tools/line_field_capacity_sim.py`(入库,忠实复刻烘焙+FS 解算)。
数据:重庆 z14 最密三瓦(932/642/641 线,3572/3168/3154 段)。
**判据:只统计穿透 ±0.5px AA 带的错误**——FS 用 `smoothstep(w+0.5px, w-0.5px)`
做抗锯齿,亚像素级偏差本就不可见;用硬阈值统计会把它算成"漏画",
虚高一个数量级(0.040% → 0.380%)。分母 = 该画的像素数。

**漏画率随距离段(texelPx = 2^d,d = 影像页z − 场页z):**

| | 半宽 1.05px(z12 端) | 半宽 2.0px | 半宽 3.15px(z16 端) |
|---|---|---|---|
| texelPx=2 | 0.000% | 0.000% | 0.000% |
| texelPx=4 | 0.346% | **0.055%** | 0.002% |
| texelPx=8 | 1.091% | 0.811% | **0.303%** |
| texelPx=16 | — | — | **1.484%** |

粗体 = 实际会同时出现的组合(线宽 ramp 挂 style zoom,深放大必配最粗档,
所以左下角"细线+大 texelPx"是不可达格)。**幽灵全表 0.000%。**

**三条结论:**

1. **实际最坏 ≈ 1.5%,发生在 z19 级深放大**;常用段(d≤2)≤0.06%。
2. **量化与端点夹皆无责**:去掉 RGBA8 量化 0.055%→0.058%(噪声内),
   去掉 `kLineFieldClampMaxTexels=1.5` 端点夹**数字一字不变**。
   → 这两个常数尺寸正确,不是债,别去调。
3. 漏画中 **64-88% 是容量**(真最近要素根本不在该像素的 2×2 块里),
   其余是同要素的邻段几何复现不准。

**裁决:不修。** 双线编码(+16MB 场显存、FS 4→8 次 fetch)最多吃掉容量那
64-88%,即把 z19 的 1.48% 压到 ~0.4%,常用段从 0.05% 压到 0.01% —— 买回的量级
不值这个价。真机深放大观感报怨再启。

**已否掉的免费方案(勿再提):烘焙侧仲裁。** 思路是 stamp 时留亚军,补一趟
修复 pass,在"冠军要素在该像素 4 个 2×2 块里都另有代表"时改判亚军 ——
零显存零 FS。实测 **0.040% → 0.359%,负收益 9 倍**。
死因:**要素级冗余 ≠ 几何级冗余**。邻居纹素记的是同一条路的*另一段*,
它复现不出本地那截几何;换掉冠军是零和的,让出去的比补回来的多。
连"可证无害"的严格块覆盖判据(四块逐块验证 + 奇偶四趟应用)也救不回来。

## D. 跨平台 / 编辑

| # | 判据 | 类型 | 状态 | 代价(实测) | 证据 / 差距 |
|---|---|---|---|---|---|
| **V20** | Metal / iOS 与 GLES 表现等价 | 机制 | ❌ | 未知(无设备) | **MSL 侧场解算从未真机验证**;instanced MSL 精简 uniform 无场参数,特性滞后 |
| **V21** | 编辑要素:pick / snap / 预览三接口够用 | 机制 | ✅ | — | P2;引擎只加这三个接口,编辑器逻辑不进引擎 |
| **V22** | 编辑要素贴地(stencil 挤出体) | 机制 | ✅ | 十级量要素,stencil 三 pass 但片元少 → 不构成负载 | 现状不动(TBDR 铁律:pass 数不是成本) |

---

## E. 已判死 / 勿再提(边界)

**这一节的作用是防止日后重复提议已被否决的方案。** 要推翻需要新证据,不是新想法。

| 方案 | 死因 |
|---|---|
| 墙带 + 深度带裁剪单 pass(W) | 50-65ms 比 stencil 差 4×,且肥线晃边;结构性死因=墙面片元×重 FS |
| 独立 overlay pass 画矢量 | 实测 25-30ms,不可接受 |
| 面 / 建筑进 SDF 场 | 面要「归属+颜色」非「距离」;建筑 Nyquist 死局(10-50m 楼 vs ~10m/texel) |
| 场解算分母用 `fwidth(fieldV)` | 线心是场脊、导数→0 → 沿线心挖洞(真机复现) |
| 场解算分母用 `dFdx(sampleUv)` | span 边界回绕爆导数 → 沿 cell 网格白虚线(真机复现) |
| 矢量线宽烘进场纹素 | 与「屏幕像素恒定」冲突,已被 V3 取代 |

> TBDR 铁律(判死上述方案的依据):**pass 数不是成本,片元数 × FS 重量才是。**

---

## E2. 校对记录:立项预判 vs 实际(场线宽专项,2026-08-14)

> **这是本文「方便日后校对」的第一个真实案例。** 留着它不是为了记账,
> 是为了下次做架构预判时知道**自己在哪类判断上容易失准**。

当初立项原话的逐条兑现情况:

| # | 立项预判 | 实际 | 判 |
|---|---|---|---|
| 1 | 场语义从「烘死宽度的覆盖」改成「到中心线的距离」 | 完全照此实施(`value = 1 − dist/band`) | ✅ |
| 2 | **「宽度阈值在 FS 里按 `fwidth` 换算成真屏幕像素」** | **机制错**:`fwidth(fieldV)` 不可用——场值在线心是**脊**,脊上导数→0,eps 兜底把线心判成"远"→ 沿线心周期性挖洞。正解=对**几何量 g**(跨 cell 连续的仿射)取屏幕导数 ÷span,不能对场值取 | ❌ |
| 3 | 近远景、祖先页全部自动补偿 | 兑现,四态实测 ~4px | ✅ |
| 4 | 真像素一致,超过 maplibre RTT | 兑现,且今日用 `.ref` 源码复核了 maplibre 的病灶(见水位线对照表) | ✅ |
| 5 | 代价:场编码重设计 | 如实(烘焙编码 + 单测改写 + 契约守卫指纹) | ✅ |
| 6 | 分级宽度语义要另找载体 | 预判准确,步2 落在 FS uniform ramp + cellZoom。**额外发现**:合批路径必须**逐实例**携带 cellZoom(批级 uniform 承首实例会在批内异 zoom 瓦片上重新造出台阶)——当初没预见到的管线契约面 | ✅+ |
| 7 | 估 1~2 天 | 实际 1 天(四步 + 真机验证 + 本文档) | ✅ |
| 8 | **风险:R8 量化噪声被除以小 fwidth 放大** | **风险没兑现**(band=8 下无可见抖动),**但翻了两个没预见的车**:① 上面 #2 的场脊挖洞;② 深放大时纹素相位让 distPx 沿线振荡过阈 → 线碎成点串(靠半宽/AA 纹素下限修) | ⚠️ |

**可迁移的教训**:

- **方案「结论」与「机制细节」要分开信任等级。** 结论(改距离场 → 真像素一致 →
  超越 RTT)经论证可信且全中;但其中「用 `fwidth` 换算」这一步的**具体机制是错的**。
  今天若盲信立项文照抄,会直接卡在挖洞上并误以为是 R8 精度问题。
- **风险预判会瞄错靶。** 事前担心的(R8 噪声)没出事,出事的两条(场脊导数为零、
  纹素相位振荡)事前完全没想到——**都属于"纸面推不出、真机一眼看见"那类**。
  这是继续保留「PoC 先行 + 真机截图验收」的直接理由。

---

## E3. 复查记录:兜底路径冒充主路径(2026-08-14)

**触发**:讨论 V5 时实测 `d`(场纹素放大级数)的分布,发现问题范围远超判据描述。

### 实测数据(默认 30km 视角,release,真机)

```
V5DIAG fieldPages=25  d0=480 (8%)   d1=0   d2=5220 (92%)   d3+=0
```

`d = 影像页zoom − 场页zoom`,`texelPx = 2^d`。d=2 → 场纹素 = 4 屏幕像素。

### 后果链(一个根因,三条判据同时假绿)

```
d=2 (92% 画面)
 ├─ wEff = max(ramp 1.05~3.15, 0.6×4=2.4) → 取 2.4 常数 ⇒ V3 假恒定 + V4 分级失效
 └─ aa   = max(0.5,            0.35×4=1.4) → 取 1.4       ⇒ V5 软
```

**连带解释了两件历史误判**:
- 「线太粗」→ 我调细三档 ramp(`368b9cce4`),但那 92% 的画面根本不看 ramp,粗细由兜底定;
- 步2 收官测「远/中/近线宽都差不多」→ 我归因成"斜线扫描测量偏差",**实际是真的没分级**。

### 兜底点全面审计(整条场链)

只有上述两个是**功能性**兜底(常态生效);其余(`max(rms,1e-6)`、`clamp(uvIn)`、
`clamp(cell)`、`fz` 的 df≤6 约束等)均为防御性,正常路径不触发,已逐个确认。
`band=8 texel`(d=2 时覆盖 32px)与 R8 量化(0.125 屏幕像素/级)均已排除。
**根因唯一:标量距离场在放大时无法被双线性插值正确重建。**

### 补充证据:主路径失效的程度(数值模拟,2026-08-14)

`scratchpad/vdf_sim.py` 在 texelPx=4(=实测 d=2)下,用**真实解算参数**
(半宽 1.2px、AA 0.5px、**兜底去掉**)重建各类几何:

- **标量场 +0.36px 系统性高估(100% 高估),渲染出来是一片空白** ——
  即当前实现在 92% 的画面里,**主路径完全画不出线**,屏幕上能看见路网
  全靠 `0.6×texelPx` 把阈值抬到 2.4px。不是"部分失效",是彻底失效。
- 高估的机制:距离场在线心是 V 形尖点,双线性插值跨尖点必然取到两侧的
  较大值 → 线被"吃掉"。这是标量距离场放大时的**固有性质**,不是参数问题。

### 元规则(新增,适用于所有判据)

> **带兜底 / 下限 / clamp 的机制,验收必须证明兜底没有生效**——只看"结果没崩"
> 会把兜底路径的稳定性当成主路径的正确性。V3/V4/V5 三条判据同时踩这个坑。
> 落实方式:这类判据的验收要附一个**主路径命中率**(如本次的 d 分布直方图)。

---

## F. 待你拍板的开放项

| 项 | 我的建议 | 影响的判据 |
|---|---|---|
| **场重建精度**(最高优先;V3/V4/V5 是同一根因,一次修三条) | **方案空间已系统探索**(真实路网数值模拟,`tools/vdf_fidelity_sim.py`;texelPx=4 = 实测 d=2,真实解算参数、兜底去掉):<br><br>**根本矛盾 = 只要"对编码值做双线性插值"重建,总有某类几何撞在插值枪口上**(标量:跨线心尖点必高估;向量:跨中轴必过零)。正确方向是**不插值**。<br><br>真实路网对拍(重庆 z14 最密子块,漏画占真值线 / 有害幽灵=离线边>0.8px 占全屏):<br>· 标量场(现):**漏画 63.1%** / 0.08% —— 主路径彻底失效<br>· **C 向量场 RG8**:漏画 3.0% / 0.30% —— 插值近似,平行近线(16% 路段,双向车道)中轴出幽灵<br>· D 线方程纹素(初版):漏画 0.4% / 有害幽灵 0.13%,误差 0.124px —— 无插值方向正确,但无限直线截断与端点无关 → 路口胡须+深放大拐点折线<br>· **D2 线段纹素(定稿)**:每纹素存**线段**而非直线——最近点偏移(±2.5texel,8bit×2)+方向角(8bit)+**到段两端的剩余长度(4bit×2)**,恰满 RGBA8;FS 胶囊在真实端点精确收口,拐角两胶囊圆帽相接=天然圆角。**MVT 折线段内本就是直线 → D2 = 精确重建,仅剩量化误差**。实测:d=2 漏画 0.28% / **有害幽灵 0.000%** / 误差 **0.025px**;d=3(深放大)漏画 0.37% / 有害 0.000% / 0.051px——**路口毛刺与拐点折线双双归零**,残余仅为单纹素单线容量(最密路口核心 ~0.3%)<br>· 已否:C′ 混合编码(高估回归全局)/ A 提封顶(爆页数吐 V14)/ B 提密度(治标)/ FS 遍历线段(重 FS,TBDR 死)<br><br>**我押 D2 主攻**:保住寄生架构与步3 全部收益,改动=烘焙编码+FS(~1 天量级)。代价:场纹理 R8→RGBA8(实测 25 页 1.6→6.5MB)、FS 4 次 texelFetch+4 组胶囊距(~15 ALU/组,仍轻)、采样改 NEAREST/texelFetch 语义。<br>**降本二项(随 PoC 一起做)**:①空邻域哨兵早退(烘焙时给"自身+3×3 邻域无线"的纹素写哨兵,FS 首 fetch 撞哨兵即退)→ 平均 fetch 4→~1.6,FS 增量 ×3→×1.5;②场页容量 160→64 层(实测驻留 25 页,2.5× 余量)→ 显存封顶 40→16MB。**已否的降本**:d+θ 紧凑编码(偏移⊥方向,理论上省 1 字节)——模拟显示误差翻倍且幽灵回潮(0.048px/0.048%),因 θ 量化误差与锚点耦合放大;(ox,oy) 的冗余实为误差解耦,保留。RG8 双字节/单 fetch 均需吐质量,不做。<br>**E(几何线条带+VS 采高贴地)立为战略备选**:maplibre 平面模式移植,线宽数学无损、dash/样式全解锁(V9 免费),复用 E1 瓦片桶与位移地形同源高度纹理;代价=放弃寄生加一个轻 pass+贴地细分新失败面(~2-3 天)。**历史"判死"只有一句带过从未实测,且当时不知场精度天花板——按判死条款,新证据成立,允许重开。** 若 D 真机观感不过关或样式需求上来,E 是终局形态。<br>PoC 验收门槛(真机像素,归你):最密路口核心的容量残余(~0.3%)+ 双向车道观感;模拟已证毛刺/折线归零,真机主要验 GPU 采样与量化实现 | V3 / V4 / V5 / V9 |
| **C 落地后 ramp 必须重调**(与 C 同批做,不可分两次) | 下限失效后线宽会掉到 ramp 真值(1.05–3.15px),观感会突然变细——你此前所有宽度判断都是在"兜底接管"状态下做的,那批判断需要作废重来 | V4 |
| 分级宽度 ramp 停点与端点 | 现 z12(0.6css)→z16(1.8css);需你手势拉远拉近看观感 | V4 |
| 线色与不透明度 | 现 (0.95,0.95,0.90,0.85) 米白 | V2 |
| V9 dash 优先级 | 我建议押后:实线已可用,dash 是样式丰富度不是体验缺口 | V9 |

---

## 更新协议

- **谁改**:每次专项收官时由我改,附 commit / 数据 / 截图。
- **何时新增判据**:你提出新的体验要求时立刻加一条(哪怕状态是 ❌),
  不要等做完再补——**没写下来的要求会在下一次会话丢失**。
- **改了体验就要更代价**:任何改动若新增 GPU/CPU/内存开销,同一次提交里更新
  「代价」列;测不了就进 C.5 记债(带编号),**不许留空**。
- **不做定期 review**:定期 review 的文档会腐烂成仪式;
  这份文档只在「有事发生」时动。
