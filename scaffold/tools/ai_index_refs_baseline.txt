# AI_INDEX.md 行号引用校验的例外清单。
#
# 这里**大部分不是文档错误,是校验器锚不住**。判据要求引用落在「本行反引号点名、
# 且能在该文件中解析出定义范围」的符号内;而文档合法地引用着构造函数、函数体内
# 的语句、GLSL 字符串常量、`// Step 2:` 这类分段注释 —— 它们都不构成"符号的
# 定义范围"。标了「已回读,正确」的都逐条读过源码确认无误。
#
# 真欠账用「真欠账」标出。本文件应当只减不增:
#   改对一条引用 → 它自然通过检查,从这里删掉
#   增强校验器让它锚得住 → 同样从这里删掉
#
# 每行 = 源文件:引用行号:符号位(`-` = 本判据不按单符号归属)
AndroidPlatformBridge.cpp:32:-
AndroidPlatformBridge.cpp:60:-
AtmosphereBackgroundPass.cpp:20:-
BoundingCylinderRegion.cpp:79:-  # 构造函数,索引不建范围 —— 已回读,79 行确是 ctor 首行
Camera.cpp:18:-  # Camera::Camera() 构造函数 —— 已回读,正确
Camera.cpp:67:-  # Camera::projectionMatrix 定义首行 —— 已回读,正确
CameraController.cpp:146:-
CameraController.cpp:27:-
CurlMultiRequestScheduler.cpp:117:-
EarthEngineSdkFacade.cpp:402:-
EllipsoidTerrainContentProvider.cpp:202:-
EllipsoidTerrainContentProvider.cpp:261:-
Engine.cpp:210:-
GltfContentProvider.cpp:3312:-
GltfContentProvider.cpp:3312:-
RasterMappedToTilesetTile.cpp:204:-  # 指向 `// Step 2:` 分段注释,不是函数定义 —— 已回读,正确
RasterMappedToTilesetTile.cpp:346:-  # 同上,指向分段注释 —— 已回读,正确
RenderContentRasterOverlayStateUpdater.cpp:105:-
RenderContentRasterOverlayStateUpdater.cpp:121:-  # ⚠️真欠账:文件已重构成 33 行纯委托,该行号无对应内容(正文已加订正说明)
RenderContentRasterOverlayStateUpdater.cpp:67:-
RenderContentRasterOverlayStateUpdater.cpp:92:-
Renderer.cpp:1061:-
Renderer.cpp:3998:-  # makeTileGeometry 定义首行(静态自由函数)—— 已回读,正确
Renderer.cpp:4126:-  # Renderer::initialize 定义首行 —— 已回读,正确
Renderer.cpp:4436:-  # 函数体内语句 cmd.vertexStride=32,本行无函数名可锚 —— 已回读,正确
Renderer.cpp:4464:-  # 同上,vertexStride=120 —— 已回读,正确
Renderer.cpp:4523:-  # 同上,instanceStride —— 已回读,正确
SceneRenderPipeline.cpp:132:-
SkyBox.cpp:45:-  # kSkyBoxStarfieldVert GLSL 字符串常量首行 —— 已回读,正确
SkyGradient.cpp:125:-
SkyGradient.cpp:164:-
SkyGradient.cpp:181:-  # 函数体内 horizMassFactor=38.0 —— 已回读,正确
SkyGradient.cpp:201:-  # 函数体内 secondaryScattering —— 已回读,正确
SunDirection.cpp:23:-
TileKey.cpp:34:-
TileSelectionTraversalExecutor.cpp:213:-  # 函数体内语句,本行是散文步骤号,无符号可锚
XYZImageryProvider.cpp:42:-  # logAndroidXyzFailure 定义首行,该名未在文档行里反引号 —— 已回读,正确
