# 接缝「漏天缝」客观度量

把「瓦片接缝观感不佳」从肉眼判断变成**可计数指标**，让接缝类改动能像加载期一样
做 A/B（见 [`tools/load_ab`](../load_ab/README.md)）。

## 为什么需要它

接缝是运动期暂态、位置随机、单帧眼看极易误判。本次排查就连续踩了两次：

1. 把 `DebugImageryProvider` 自己画的 **1px 白色瓦片边框**当成了缝；
2. 凭一张截图判定 fill 代理是横断的成因，A/B 直接证伪。

## 原理

一列像素自上而下扫，第一个非天色像素即该列地平线。**地平线以下再出现天色像素 =
地面开了缝**，天/雾从缝里透出来。天色取每帧顶部 5% 的中位色（雾随视角变，不能
写死常量），地平线下 30px 内不计（抗锯齿与雾过渡带）。

## 用法

```bash
tools/seam_metric/seam_leak.py <截图目录> [更多目录...]
```

每个目录取其中的 `s*.png`，输出漏天像素合计 / 每帧均值 / 最差帧。截图用
`adb exec-out screencap -p`，运动脚本参考 `tools/load_ab/collect.sh`。

## 已采基线（2026-08-03，掠视 1.5km→120km 缩放，release，冷缓存）

| 变体 | 漏天像素/帧 | 相对基线 |
|---|---|---|
| 封缝带 `0.30` | **53.0** | **0.53×** |
| 基线：封缝带 `0.03` | 100.8 | 1.00× |
| 「斜裙」尝试（已撤） | 640.1 | 6.4× ❌ |
| 关掉 fill 代理 | 1333.1 | 13.2× ❌ |

三条结论都由这张表定的，不靠观感：

- **fill 代理是承重的**，关掉漏天缝涨 13 倍（它在堵洞，不是接缝的成因）；
- **把裁剪矩形外的顶点压到椭球面当「裙」是回归**（斜裙铺开一整格宽，粗祖先一格
  可达 ~10km，翻出大片平板），已撤回；
- **横向封缝带 0.03 → 0.30 有效**，但那是把覆盖量调大，治标。

⚠️ 跨**构建变体**比较要小心：关掉卫星影像改走 Debug 棋盘时，细分深度由 debug
图层驱动（实测到 z16+），可见瓦片数与 LOD 跨度都变了，与卫星档不可直接比。
同一档内比才有效。

## 采集脚本化(2026-08-03)

暂态接缝峰值随缓存温度/加载节奏剧烈波动(同构建实测 1260 vs 6699),单轮
采样不可判 A/B。`collect.sh` 固化协议:冷缓存(卸载重装)× N 轮 × 同段确定
性运动(掠视 + 缩放往返),含真人触摸污染检测;`report.py` 三指标判定:

| 指标 | 判据 |
|---|---|
| ① steady(尾段 40% 最大漏天) | **必须全轮 = 0**,回归一票否决(机制 B 已达成) |
| ② transSum(全序列合计) | ≤1.2× 基线;超且 p<0.05 FAIL;超但 p≥0.05 INCONC |

```bash
tools/seam_metric/collect.sh before 3
# 改代码 → 重建 release
tools/seam_metric/collect.sh after 3
tools/seam_metric/report.py tools/seam_metric/out/before tools/seam_metric/out/after
```

## 金字塔层间 ε 实测(`pyramid_eps.py`)

机制 B 后暂态残缝 = 金字塔层间重采样差 ε(自吸附只能精确到"自数据降采样",
父层 ≠ 子层降采样时残缝即 ε)。线上源(重庆山区)实测:**边界 p95 = 5-13m,
峰 25-50m** —— 掠视下数像素,与观测吻合。

数据侧解:`dem_test/scripts/build_nested_pyramid.py` 把粗层重建为细层的
2×2 均值聚合。线上子树验证:重建后 ε → **mean 0.02m / max 0.05m**(0.1m
编码步长的舍入上界,理论极限)。待 hosting 侧全量重建后,用上面的 A/B 协议
验收暂态归零。

### 已采基线(2026-08-03,机制 A+B 构建 `a0a3674df`,冷缓存 ×3)

| 轮 | steady | transSum | transPeak |
|---|---|---|---|
| run1 | **0** | 2260 | 659 |
| run2 | **0** | 80666¹ | 78436¹ |
| run3 | **0** | 2071 | 659 |

steady 三轮全 0(机制 B 在脚本化协议下复核成立)。¹run2 野值 = **系统推送
横幅**恰好盖住截图(白色横幅被判天色),非接缝;中位数(2260)已吸收。

**协议坑(都踩过,已进脚本注释)**:
- 俯仰键方向:DPAD_UP(19)=压向掠视,DPAD_DOWN(20)=抬回俯视,写反 = 全程
  正俯视无地平线,检测器整段误报(steady 39 万全是假);
- `adb exec-out screencap` 偶发悬挂 10+ 分钟,必须超时重试(shot());
- 污染向量②:推送横幅。采集前建议开勿扰;report 靠中位数兜底;
- 等待采集结束别用 `pgrep -f`(会匹配等待命令自身),认 stdout 的"完成"标记。
